apiVersion: v1
kind: Pod
metadata:
  name: apache-pod
  labels:
    app: nginx-app
  annotations:
    description: 'This is nginx-pod2'
spec:
  containers:
    - name: apache-container
      image: httpd
      ports:
        - containerPort: 80