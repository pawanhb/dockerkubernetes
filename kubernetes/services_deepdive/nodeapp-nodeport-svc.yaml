apiVersion: v1
kind: Service
metadata:
  name: nodeapp-nodeport-svc
  labels:
    app: node-app
spec:
  type: NodePort
  selector:
    app: node-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32767 
