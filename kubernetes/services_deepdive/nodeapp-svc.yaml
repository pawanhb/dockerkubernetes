apiVersion: v1
kind: Service
metadata: 
  name: nodeapp-svc
  labels:
    app: node-app
spec:
  selector:
    app: node-app
  type: ClusterIP
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
    
