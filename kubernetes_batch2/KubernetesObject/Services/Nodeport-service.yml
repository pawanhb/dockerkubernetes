apiVersion: v1
kind: Service
metadata:
  name: clusterip-service
  labels: 
    app: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx-pods
  ports:
  - port: 80
    protocol: TCP
    nodePort: 30080  ##Always between 30000–32767.
    targetPort: 80