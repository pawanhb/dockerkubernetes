apiVersion: v1
kind: Pod
metadata:
  namespace: dev-ns
  name: dev-pod
  labels:
    app: my-app
    environment: dev
spec:
  containers:
  - name: myapp
    image: nginx
    ports:
      - containerPort: 80
