apiVersion: v1
kind: Pod
metadata:
  name: myapp-backend
  labels:
    app: myapp
    tier: backend
    environment: prod
    release: canary
spec:
  containers:
  - name: myapp
    image: nginx
    ports:
      - containerPort: 80
