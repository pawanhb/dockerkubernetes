apiVersion: v1
kind: Pod
metadata:
  name: pod-toleration-demo
  labels:
    app: pod-toleration
spec:
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "special"
    effect: "NoSchedule"
  containers:
  - name: nginx
    image: nginx
