apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-volume-pvc
spec:
  resources:
    requests:
      storage: "2Gi"
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: standard